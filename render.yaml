services:
  - type: web
    name: planillas-cof-web
    env: python

    buildCommand: |
      pip install -r backend/requirements.txt

    startCommand: |
      uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT